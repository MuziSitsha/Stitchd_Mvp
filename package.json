{
  "name": "stitchd-platform",
  "version": "1.0.0",
  "private": true,
  "description": "STITCHD - On-Demand Services Platform for South Africa",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "yarn workspace @stitchd/api dev",
    "dev:admin": "yarn workspace @stitchd/admin dev",
    "build:api": "yarn workspace @stitchd/api build",
    "build:admin": "yarn workspace @stitchd/admin build",
    "preview:mobile:web": "py -3 apps/mobile/tool/preview_web_build.py",
    "lint": "yarn workspaces run lint",
    "test": "yarn workspaces run test",
    "db:migrate": "yarn workspace @stitchd/api migration:run",
    "db:seed": "yarn workspace @stitchd/api seed:run"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "eslint": "^8.57.0",
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=20.0.0",
    "yarn": ">=1.22.0"
  }
}
